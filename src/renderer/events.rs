//! Placeholder module for future event streaming features.
//!
//! Planned features:
//! - WebSocket server to export the graph as JSON (web dashboard)
//! - File-based event log export (JSONL format)
//! - Prometheus metrics endpoint

// Currently empty — rendering events flow through renderer::Renderer directly.
